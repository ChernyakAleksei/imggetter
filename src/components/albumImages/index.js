import ng from 'angular';
import albumImagesComponent from './albumImages.component';

export default ng.module('app.components.albumImages', [])
    .component('albumImages', albumImagesComponent).name;
