import ng from 'angular';
import singleImagesComponent from './singleImage.component';

export default ng.module('app.components.singleImage', [])
    .component('singleImage', singleImagesComponent).name;
